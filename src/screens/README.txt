Simple screen names for easy navigation:

- WelcomeScreen.jsx -> landing page
- HomeScreen.jsx -> user dashboard
- GradeTrackerScreen.jsx -> public CGPA tracker
- MyGradeTrackerScreen.jsx -> logged-in user CGPA tracker
- TaskPlannerScreen.jsx -> to-do planner

These files are simple wrappers around existing pages so the app stays stable.
