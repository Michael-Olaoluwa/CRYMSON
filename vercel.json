{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "react",
  "env": {
    "REACT_APP_API_BASE_URL": {
      "description": "Backend API base URL - use your Railway backend URL",
      "required": true
    }
  }
}
